import { defineConfig } from '@apps-in-toss/web-framework/config';

export default defineConfig({
  appName: 'pay-or-nopay',
  brand: {
    primaryColor: '#1a1a2e',
  },
  permissions: [],
  webBundleDir: 'out',
});
